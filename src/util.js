console.log('im util running!!!!');

export const SQR = (number) => number * number;
export const ADD = (x, y) => x + y;
//const SUB = (x, y) => x - y;
export default (x, y) => x - y;
//export { SQR, ADD, SUB as default };